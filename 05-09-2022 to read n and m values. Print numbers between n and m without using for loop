 write a program to read n and m values. Print numbers between n and m without using for loop
 
 #include <stdio.h>
int print(int n,int m);
int main()
{
     print(4,10);
}
int print(int n,int m)
{
    if(n>m)
    return 0;
    else
    printf("%2d ",n);
    print(n+1, m);
}

OUTPUT:

 4  5  6  7  8  9 10
